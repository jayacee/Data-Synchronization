# Data-Synchronization
Simple proof of concept for a tool that synchronizes data in a directory across 2 computers.

To get started, run ```server.py``` and then run ```sync_tool.py -d client_replicated```

Any changes made in ```client_replicated``` will be copied to ```server_replicated```
